<form method="post" action="{{ path('app_admin_banned_delete', {'id': user_banned.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_banned.id) }}">
    <button class="">Supprimer</button>
</form>
