{% extends 'base.html.twig' %}
    {% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('asset/css/form.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/admin.css') }}">
{% endblock %}
{% block title %}UserBanned{% endblock %}

{% block body %}
    <h1>UserBanned</h1>
    <div class="create-new-container">
    <a href="{{ path('app_admin_banned_index') }}" class="create-new-link">Retour à la liste</a>
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_banned.id }}</td>
            </tr>
            <tr>
                <th>BannedDate</th>
                <td>{{ user_banned.bannedDate ? user_banned.bannedDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NumberBan</th>
                <td>{{ user_banned.numberBan }}</td>
            </tr>
            <tr>
                <th>IsPermanentlyBanned</th>
                <td>{{ user_banned.isPermanentlyBanned ? 'Oui' : 'Non' }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('admin_banned/_delete_form.html.twig') }}
{% endblock %}
