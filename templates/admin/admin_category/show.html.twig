{% extends 'base.html.twig' %}
    {% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('asset/css/form.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/admin.css') }}">
{% endblock %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>
    <div class="create-new-container">
    <a href="{{ path('app_admin_category_index') }}" class="create-new-link">Retour à la liste</a>
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ category.label }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('admin/admin_category/_delete_form.html.twig') }}
{% endblock %}
