{% extends 'base.html.twig' %}

{% block title %}Championnat index{% endblock %}

{% block body %}
    <h1>Championnat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Country</th>
                <th>Ligue</th>
                <th>Code_api</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for championnat in championnats %}
            <tr>
                <td>{{ championnat.id }}</td>
                <td>{{ championnat.country }}</td>
                <td>{{ championnat.ligue }}</td>
                <td>{{ championnat.codeApi }}</td>
                <td>
                    <a href="{{ path('app_admin_championnat_show', {'id': championnat.id}) }}">show</a>
                    <a href="{{ path('app_admin_championnat_edit', {'id': championnat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_championnat_new') }}">Create new</a>
{% endblock %}
