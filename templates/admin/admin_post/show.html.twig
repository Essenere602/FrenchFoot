{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('asset/css/form.css') }}">
    <link rel="stylesheet" href="{{ asset('asset/css/admin.css') }}">
{% endblock %}
{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>
    <div class="create-new-container">
    <a href="{{ path('app_admin_post_index') }}" class="create-new-link">Retour à la liste</a>
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ post.label }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ post.message }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('admin/admin_post/_delete_form.html.twig') }}
{% endblock %}
